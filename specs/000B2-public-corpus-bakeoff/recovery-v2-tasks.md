# 000B2 ATTEMPT-003 Recovery Tasks

This ledger is the successor recovery chain selected only through `specs/CURRENT.md` and `research/000b2-public/recovery-attempt-003-readiness.json`.

The B2R01-B2R12 ledger in `tasks.md` remains historical chronology. B2R10-B2R12 are not completed; they are superseded because ATTEMPT-002 is materially invalidated.

## Successor recovery

- [x] `B2R13` — Qualify and canonically bind ATTEMPT-002 material invalidation against exact canonical B2R09 bytes and the pinned sherpa-onnx upstream API. Preserve all historical attempt bytes. No primary decode.
- [x] `B2R14` — Qualify a corrected sherpa-onnx result-extraction harness against non-primary material only, including fail-closed string/object API regression coverage. No primary decode.
- [x] `B2R15` — Capture or cryptographically rebind ATTEMPT-003 preprocessing and execution-environment evidence without changing candidate/subset/scorer/normalization/C0 identities. No primary decode.
- [ ] `B2R16` — Freeze `000B2-PUBLIC-ATTEMPT-003` with exact candidate, subset, preprocessing, environment, scorer, normalization, and harness identities before any new primary decode.
- [ ] `B2R17` — Execute ATTEMPT-003 candidate cell 1, `moonshine-compact`, against the identical frozen P0 audio; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R18` — Execute ATTEMPT-003 candidate cell 2, `moonshine-balanced`, against the identical frozen P0 audio; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R19` — Execute ATTEMPT-003 candidate cell 3, `whispercpp-compact`, against the identical frozen P0 audio; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R20` — Execute ATTEMPT-003 candidate cell 4, `whispercpp-balanced`, against the identical frozen P0 audio; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R21` — Execute ATTEMPT-003 candidate cell 5, `sherpa-onnx-compact`, using the exact B2R14-qualified result-extraction harness; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R22` — Execute ATTEMPT-003 candidate cell 6, `sherpa-onnx-balanced`, using the same exact B2R14-qualified result-extraction harness; preserve raw outputs, failures, runtime observations, run identity, and claim guards. No scoring.
- [ ] `B2R23` — Preserve and verify the complete ordered ATTEMPT-003 raw transcript/failure/runtime/run-identity evidence set for all six cells. Prove identical frozen attempt binding. No scoring.
- [ ] `B2R24` — Score ATTEMPT-003 only under the frozen scorer and public normalization contract after B2R23 is canonical. Preserve `HUMAN_DEVELOPER_SPEECH_ACCURACY_EVIDENCE=ABSENT`; do not select a production STT or authorize product code.

## Permanent task guards

For every successor unit:

- only the exact active task is executable;
- no stale CI/review evidence transfers after a head change;
- independent substantive review is mandatory before merge;
- failed or partial evidence remains visible;
- historical ATTEMPT-001 and ATTEMPT-002 bytes remain immutable;
- result-driven candidate, subset, scorer, normalization, or C0 changes are prohibited;
- `production_stt_selected=false`;
- `product_code_authorized=false`.
