# Synthetic ACP Probe Fixture

This repository fragment exists only for Wispral Specification 000A protocol qualification.

It contains no user data, credentials, production source, network configuration, or secret material.

## Deterministic fact

The marker in `probe.txt` is exactly:

`WISPRAL_ACP_PROBE_OK`

A future authenticated non-destructive prompt may ask an agent to report that marker without modifying files. No authenticated prompt is authorized by the unauthenticated CI probe.
